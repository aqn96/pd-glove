# Images Directory

This folder contains all repository image assets (prototype photos, architecture diagrams, and dashboard mockups).

Use images from markdown files under `docs/` with:

```markdown
![Alt text](../images/file-name.png)
```
